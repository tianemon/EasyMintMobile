# Changelog

> **编辑约定**（与桌面端 EasyMint 同一套口径）：只记录**用户可见变更**——新增功能 / 行为改进 / 修复的现象，
> 不写机制与实现细节。条目格式：`**标题**：一句话说清用户能感知的变化`。
> 版本号走 SemVer：新增功能 → 中版本号，纯修复 → 小版本号。
> 日常变更先记入下方 `[Unreleased]`，发版时整理成本版本条目（发版流程见 `.github/workflows/release.yml`）。

## [Unreleased]

## [v0.1.0] — 首个版本：扫码配对、会话操作与附件

### Added（新增）

- **扫码配对**：扫描桌面端「设备互联 → 手机终端」的二维码，核对六位数字后在电脑上确认，即可与桌面端建立加密连接
- **会话实时操作**：在手机上打开项目、切换会话、发消息；任务运行中还能继续插话引导，随时中止
- **过程可见**：模型的思考、工具调用与执行输出实时呈现，长内容可折叠与横向滚动
- **附件发送**：可随消息发送图片与文档——图片走系统相册，文档走系统文件选择器；单文件与单次总量上限 15 MB、单次最多 10 个
- **输入卡控制项**：会话内可切换权限模式（只读 / 标准 / 完全访问）、切换模型与思考等级，并查看上下文占用
- **会话管理**：重命名、置顶、归档与恢复
- **互动与后台可见**：桌面端向你提问时可直接在手机上作答；后台命令与子 Agent 委派进度可查看、可停止，后台命令的完整日志能打开阅读

### Changed（变更）

- **同一回合合并成一个气泡**：思考、多个工具调用与正文不再各自一个气泡，与桌面端显示口径一致
- **附件菜单改为两个按钮**：图片在上、文档在下，替掉原先的系统弹窗
- **连接状态更直观**：标题栏用灯色表示与电脑的连接状态

### Fixed（修复）

- **iOS 27 启动即崩**：改用系统要求的 UIScene 生命周期，修复在 iOS 27 上「点开就闪退」
- **附件被拒**：手机端补齐与桌面端一致的数量上限（单次 10 个），不再出现「选了一堆小文件却整条发不出去」
