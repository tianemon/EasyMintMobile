# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision

# Firebase 配置（含项目 ID 与 API key）
google-services.json
GoogleService-Info.plist

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# 原生工程由 expo prebuild 生成并在每次打包前重建（见 scripts/build-apk.mjs），不入库
/android
/ios

# 打包产物
/apk/*.apk
/ipa/*.ipa
*.pem

# 本地助手的工作目录（项目记忆、日志），不入库
.workbuddy/

# EasyMint harness 的本地固化命令，不入库
.easymint/

# local env files
# 模板文件（.env.example）仍可入库，其余 .env* 一律不进仓库
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo
